# Core
numpy>=1.26
pyyaml>=6.0
tqdm>=4.66

# Video decode / image ops (has cp313 wheels)
opencv-python>=4.10

# Deep learning. Install torch/torchvision with a CUDA build separately, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# The bare names below let pip resolve a CPU build if you skip the CUDA index.
torch>=2.5
torchvision>=0.20

# Teacher backbone (V-JEPA-2) is loaded via HuggingFace, only used offline.
transformers>=4.44
timm>=1.0
safetensors>=0.4

# Export / deployment
onnx>=1.16
onnxruntime>=1.18
onnxscript>=0.7                # enables torch's dynamo-based ONNX exporter
